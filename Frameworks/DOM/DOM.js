// Since DOM is already implemented in HTML, this framework has no generic sources.
// - The Node variant populates a global object named DOM with apis matching
//   what HTML environments have on window
// - The HTML variant simply maps DOM to window