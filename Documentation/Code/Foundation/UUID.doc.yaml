kind: function
introduced: "1.0.0"
summary: Create a UUID version 4 string
description: |
  A UUID is a random identifier that should be globally unique because it based
  on the current time and large random number.